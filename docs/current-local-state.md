# tutuorders — текущее состояние (локально)

Документ описывает, **что сейчас работает** при запуске на `http://localhost:5173`.  
Обновляйте при заметных изменениях в UI или API.

**Не путать с:** `docs/variant-b-schema-and-screens.md` — это **план развития**, а не текущая версия.

---

## Запуск

| | |
|--|--|
| Сайт | http://localhost:5173 |
| API | http://127.0.0.1:8000 (прокси с Vite `/api` → :8000) |
| Команда | `.\start-local.cmd` из корня проекта |
| БД | `backend/app.db` (SQLite) |
| ИИ | `backend/.env` — YandexGPT; без ключей работает **fallback без ИИ** |
| Вход | `AUTH_ENABLED=false` по умолчанию — логин на локалке не нужен |

Корень проекта:  
`C:\Users\chugi\OneDrive\Документы\папочка рабочая\Документы\BD\AI\Cursor`

---

## Экраны (3 вкладки)

### Прайсы

- Список **всех** поставщиков из БД (изначально 2 из seed, можно **добавлять новых**).
- Карточка: имя, минимальный заказ (RUB), сохранение отдельно от прайса.
- Загрузка `.xls` / `.xlsx` (колонки A название, B единица, C цена) — **заменяет** старый прайс поставщика.
- «Загружено позиций: N · последняя загрузка: …» + зелёная плашка после только что загруженного файла.
- Блок **«Новый поставщик»** — создание через API `POST /api/suppliers`.

### Заказ

- Текстовый список: `Название количество единица` (`кг`, `г`, `гр`, `л`, `мл`).
- **Проверить ввод** — парсинг, превью таблицы, пересчёт `г/гр → кг`, `л → мл`, колонка «Пересчёт».
- **Сопоставить** — долгий запрос к `/api/match` (ИИ + fallback).
- Текст заказа и превью **не сбрасываются** при переходе на «Результат» и обратно.
- **Очистить** — новый список с нуля.

### Результат

- Таблица после сопоставления: продукт, единица, кол-во к заказу.
- Для **каждого поставщика**: колонки **Цена** и **Кол-во** (число колонок растёт с числом поставщиков).
- Строка «Итого», подсветка позиций без матча, комментарии по строкам.
- Статус: с ИИ / без ИИ (degraded mode).
- **Скачать xlsx** — кнопка **вверху** страницы; в Excel — цена, кол-во, сумма по каждому поставщику.

---

## Что хранится в БД

| Таблица | Содержимое |
|---------|------------|
| `suppliers` | поставщики, мин. заказ, `last_price_upload_at` |
| `prices` | строки прайсов (название в прайсе, ед., цена) |
| `settings` | folder_id, model_name для YandexGPT |

**Не хранится:** история заказов, канонический каталог продуктов, локации, версии комментариев к поставщикам (это в плане variant B).

---

## Сопоставление (как работает сейчас)

1. Парсер разбирает текст заказа.
2. Backend отправляет заказ и прайсы в **YandexGPT** (если настроен `.env`).
3. При ошибке/таймауте/битом JSON — **fallback без ИИ** (поиск по БД, score, мин. цена).
4. Распределение количеств и сумм; комментарий в результате — **на лету** (не отдельный справочник specs).

Оптимизация «глобально дешевле с учётом мин. заказа» и **запрет дробить продукт между поставщиками** — **ещё не реализованы** (см. variant B).

---

## API (основное)

| Метод | Путь |
|-------|------|
| GET | `/api/suppliers` |
| POST | `/api/suppliers` |
| PUT | `/api/suppliers/{id}` |
| POST | `/api/prices/upload` |
| POST | `/api/order/parse` |
| POST | `/api/match` |
| POST | `/api/export` |
| GET | `/api/health` |

---

## Чего пока нет (только в плане)

- 4 локации, кухня/бар, отдельные списки поставщикам по локациям.
- Словарь канонических продуктов и экран спецификаций.
- MILP-оптимизатор закупки.
- Обязательный матчинг-экран в wizard «План закупки».

План: [`variant-b-schema-and-screens.md`](variant-b-schema-and-screens.md)

---

## Связанные файлы

| Файл | Назначение |
|------|------------|
| [`README.md`](../README.md) | общее описание, деплой, git |
| [`.cursor/rules/local-dev.mdc`](../.cursor/rules/local-dev.mdc) | как запустить (для Cursor) |
| [`variant-b-schema-and-screens.md`](variant-b-schema-and-screens.md) | будущая архитектура |

---

*Последнее обновление документа: май 2026, ветка `fix/match-resilience-and-ux`.*
